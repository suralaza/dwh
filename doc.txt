# Описание процесса работы с GUI и конфигом

1. Первичная настройка
    - Пользователь один раз открывает GUI.
    - Заполняет все необходимые правила, шаблоны путей, параметры парсинга и другую конфигурацию.
    - Указывает путь до папки, в которую будут складываться релизные файлы.
    - Сохраняет всю конфигурацию в YAML-файл.

2. Работа с релизами
    - Для каждого нового релиза пользователь просто указывает новый путь до нужной папки с релизными файлами.
    - Все остальные правила и параметры используются из уже сохранённого YAML.
    - Парсер автоматически обрабатывает релиз на основе этих настроек.

📝 Таким образом, детальная настройка производится всего один раз, а в дальнейшем работа упрощается до минимума: нужно только выбрать/ввести актуальную папку с файлами релиза.

# Пример сценария

1. На старте проекта:  
    - В GUI заполняются шаблоны путей, правила блоков, типы объектов и прочее.
    - Конфиг сохраняется, например, как /project/release_parser_config.yaml.

2. При каждом релизе:  
    - Открываешь GUI или указываешь через CLI только путь к папке с релизными файлами.
    - Парсер подхватывает конфиг, выполняет разбор и выкладывает результат в нужные места.

---

Такой подход экономит время, снижает риски ошибок и делает процесс максимально предсказуемым. 🚀